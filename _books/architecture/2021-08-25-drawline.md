---
layout: post
title: 경계 선긋기
date: 2021-08-25 00:00:00
tags: [architecture]
author: mason
categories: [books,clean_architecture,part5,ch17]
---
## 어떻게 선을 그을까? 그리고 언제 그을까?
어떻게? : 관련이 있는것과 없는것 사이에 선을 그어서 둘을 분리시킨다.
* GUI 와 업무규칙
* DB 와 업무규칙
![인터페이스뒤로숨은데이터베이스](assets/images/clean/2021-08-25_19-01-00.png)

> 그림 17.1 에서 이 점을 분명하게 볼 수 있다. `BusinessRules` 는 `Database interface` 를 사용하여 데이터를 로드하고 저장한다. `DatabaseAccess` 는 `DatabaseInterface` 를 구현하며, `Database` 를 실제로 조작하는 일을 맡는다.

어디에 그을까? 위 그림(17.1)에서 Database Interface 아래 그어진다.

> 경계선은 어디에 있는가? 경계선은 상속관계를 횡단하면서 `Database Interface`  바로 아래에 그어진다. (그림17.2)
> `DatabaseAccess`에서 출발하는 두 화살표에 주목하자. 이들 두 화살표는 `DatabaseAccess` 클래스로부터 바깥쪽으로 향한다. 즉, 이 도표에서 `DatabaseAccess` 가 존재한다는 사실을 알고 있는 클래스는 없다는 뜻이다.

![경계선](assets/images/clean/2021-08-25_20-59-21.png)

위 경계선 구분을 의존성이라는 개념으로 보면, DB 는 업무규칙을 필요로 하지만, 업무규칙은 DB를 필요로 하지 않는다. ‘업무규칙을 실행하려면 DB를 알아야 하는것 아닌가?’ 라고 되물을 수 있지만, DB는 엄밀히 말해서 업무규칙 실행하는데 필요한 도구일뿐이다. 필요에 따라서 이것을 DB가 아니라 파일이나 다른 형태의 도구로 바꿀 수 있다.

> 이러한 사실이 이상하게 보인다면 하나만 기억하자. `Database` 컴포넌트는 `BusinessRules` 가 만들어 낸 호출을 데이터베이스의 쿼리 언어로 변환하는 코드를 담고 있다. `BusinessRules` 에 대해 알고 있는 코드는 바로 이 변환코드다.
> 두 컴포넌트 사이에 이러한 경계선을 그리고 화살표의 방향이 `BusinessRules` 를 향하도록 만들었으므로, `BusinessRules` 에서는 컴포넌트는 다양한 구현체로 교체될 수 있으며, `BusinessRules`는 조금도 개의치 않는다.

### 입력과 출력은?
시스템에서 눈에 보이는 GUI(사용자 인터페이스) 보다 더 중요한것은 모델(데이터구조, 함수로구성된것)이다. 모델에게 가장 중요한 것은 업무 규칙이며 그렇기 때문에 GUI 는 업무 규칙에 의존한다.

> 이 경우  사용자 경험은 인터페이스에 의해 좌우된다. 화면, 마우스, 버튼, 음향이 바로 그 인터페이스다. 이러한 인터페이스 뒤에는 인터페이스를 조작하는 모델(데이터 구조와 함수로 구성된 정교한 집합) 이 존재한다는 사실을 잊어버린다. 

## 플러그인 아키텍처
플러그인 아키텍처는 핵심적인 업무규칙과 분리된 여러개의 플러그인을 연결하는 구조이다.
> 사실 소프트웨어 개발 기술의 역사는 플러그인을 손쉽게 생성하여, 확장가능하며 유지보수가 쉬운 시스템 아키텍처를 확립할 수 있게 만드는 방법에 대한 이야기다. 선택적이거나 또는 수많은 다양한 형태로 구현될 수 있는 나머지 컴포넌트로부터 핵심적인 업무규칙은 분리되어 있고, 또한 독립적이다.
 
![업무 규칙체 플러그인 형태로 연결하기](assets/images/clean/2021-08-30_21-52-15.png)

## 결론
컴포넌트는 핵심업무규칙과 플러그인으로 나눈다. 플러그인은 핵심업무규칙을 참조하도록 한다.
> 나머지 컴포넌트는 플러그인으로, 핵심 업무와는 직접적인관련이 없지만 필수 기능을 포함한다. 그런 다음 컴포넌트 사이의 화살표가 특정 방향, 즉 핵심 업무를 향하도록 이들 컴포넌트의 소스를 배치한다. 
