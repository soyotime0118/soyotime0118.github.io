---
layout: post
title: 5. 소프트웨어에서 표현되는 모델
date: 2022-01-13 00:00:00
tags: [book,entity,vo,연관관계]
categories: [books,domain-driven-design-eric-evans,part2,ch5]
translator: mason
---

* [연관관계](#연관관계) : 객체간의 연관관계를 구현할때 어려운 것은 무엇인가?
* [Entity](#Entity) : 식별성있고 식별성으로 지속성을 확인할 수 있는 객체는 무엇인가?
  * [Entity 모델링](#Entity-모델링) : Entity는 어떤기준으로 모델링 해야하는가?
  * [식별연산의 설계](#식별-연산의-설계) : 식별성을 나타내는 식별연산은 어떻게 구현할 수 있는가?
* [Value Object](#Value-object) : 위 Entity에 속하지 않는것은 무엇인가?
  * [Value Object의 설계](#Value-object의-설계) : VO 를 설계할때 고려해야 하는 복사, 공유는 무엇인가? 
* [Service](#Service(서비스)) : 여러 객체에 의존성이 있고, 상태가 없는 연산은 어떻게 설계할 것인가?
* [Module](#모듈(Module)) : 모듈은 어떤 기준으로 해야 하는가?

***

## 연관관계
현실세계에서는 다대다 연관관계지만, 소프트웨어에서도 다대다 로 똑같이 구현하게되면, 구현과 유지보수가 복잡해 진다. 그러므로 설계의 단순성을 위해 가능하면 연관관계는 제약하는것이 좋다.
> 현실세계에는 수많은 다대다 연관관계가 있으며, 그중 상당수는 애초 부터 양방향으로 나타난다. … 그러나 이러한 일반적인 형태의 연관관계는 구현과 유지보수를 복잡하게 하며, 더욱이 해당 관계의 특성에 관해서는 거의 전해주는 바가 없다.


## Entity
객체는 식별성의 유무로 나눈다면, Entity는 식별성이 있는 객체를 말한다. 하지만 식별성이 있다고 해서 모두 Entity는 아니다.
> 즉 많은 것들이 속성이 아닌 식별성에 의해 정의된다는 것에 대해 알아보자.

> 개념적 식별성은 객체와 해당 객체의 저장 형태, 전화를 거는 사람과 같은 현실의 행위자(actor)의 구현 사이에서 일치해야 한다. 속성은 일치하지 않을 수도 있다.

> Entity의 근본적인 개념은 객체의 생명주기 내내 이어지는 추상적인 연속성이며, 그러한 추상적인 연속성은 여러 형태를 거쳐 전달된다는 것이다.  어떤 객체는 해당 객체의 속성을 자신의 주된 정의로 삼지 않는다. 그러한 객체는 오랜 시간에 걸쳐 작용하는 식별성의 이어짐을 나타내며, 종종 전혀 다른 형태로 나타나기도 한다.

> 어떤 객체를 일차적으로 해당 객체의 식별성으로 정의할 경우, 그 객체를 Entity라 한다. Entity에는 모델링과 설계상의 특수한 고려사항이 포함되어 있다. Entity는 자신의 생명주기동안 형태와 내용이 급격하게 바뀔수도 있지만 연속성은 유지해야 한다. 또한 사실상 Entity를 추적하려면 Entity에 식별성이 정의돼 있어야 한다.

### Entity 모델링
Entity 의 본질은 객체의 행위가 예측 가능하도록 연속성을 확립하는 것이다. 이 연속성과 관련없는 특징들은 모두 다른 객체로 이동시킨다.

> 그러나 Entity의 가장 기본적인 책임은 객체의 행위가 명확하고 예측 가능해질 수 있게 연속성을 확립하는 것이다. Entity는 별도로 분리돼 있을 때 자신의 책임을 가장 잘 수행한다. Entity의 속성이나 행위에 집중하기보다는 Entity 객체를 해당 Entity 객체의 본질적인 특정(특히 해당 Entity를 식별하고 탐색하여 일치시키는데 널리 사용되는) 만으로 정의한다.

### 식별 연산의 설계
Entity의 본질은 식별성이기 때문에 다른 객체와 구분할 수 있는 식별연산이 있어야 한다.
* 데이터 속성 혹은 여러 속성의 조합으로 식별성을 갖는다
* 식별성을 제공하는 ID를 자동으로 만든다
* 사용자에게 생성된 ID를 제공해야하는경우
* 현재 시스템의 범위를 넘어 적용돼야 하는경우 

## Value Object
모든 객체를 Entity 로 관리하게되면, 시스템의 복잡도가 증가한다. 객체중에는 식별성이 존재하지 않는 객체는 Value Object로 정의한다. VO 는 도메인의 서술적인 역할을 한다. VO 는 응집력있는 개념적 완전성을 형성해야 한다.

> 시스템에서 모든 Entity를 관리해야 한다면 여러 가지 적용 가능한 성능 최적화를 적용하지 못한다. 의미있는 식별성을 정의하고, 분산시스템에 걸쳐 있거나 데이터베이스 저장소에 들어있는 객체를 손쉽게 추적하는 수단을 마련하려면 분석적인 노력이 필요하다.

> 소프트웨어 설계는 복잡성과의 끊임없는 전투다. 그러므로 우리는 특별하게 다뤄야할 부분과 그렇지 않은 부분을 구분해야 한다.

> 개념적 식별성을 갖지 않으면서 도메인의 서술적 측면을 나타내는 객체를 Value Object라 한다. Value Object는 설계 요소를 표현할 목적으로 인스턴스ㅇ화 되는데, 우리는 이러한 설계 요소가 어느것인지에 대해서는 관심이 없고 오직 해당 요소가 무엇인지에 대해서만 관심이 있다.

> ValueObject 라고 반드시 단순하지만은 않다. 이를테면, 색 조합 프로그램에서는 기능이 풍부한 색 객체를 서로 결합해서 또 다른 색을 만들어내는 풍부한 모델을 갖추고 잇을지도 모른다.

> 어떤 Value Object는 다른 여러 객체를 조립한 것일수도 있다.

> Value Object 가 Entity를 참조할 수도 있다.

> 간혹 Value Object는 여러 객체간에 오가는 메시지의 매개변수로 전달되기도 한다. Value Object는 종종 한 연산에서 사용할 목적으로 만들어진후 폐기되는 것처럼 일시적인 용도로 사용되기도 한다. 

### Value Object의 설계
* 최적화, 설계 단순화를 위해 식별성이 필요 없는 객체는 Value Object 로 설계할 수 있다.
* VO 는 식별성이 필요 없기 때문에 복사, 공유 방법으로 최적화를 할 수 있다.
* 공유를 하게 되면, 공유중인 VO 가 변경된 경우 이를 참조하는 객체에 영향이 있기 때문에 불변객체로 설정해야 한다.
* 불변객체로 한다는 것은 VO는 수정할 수 없으며, 새로운VO 를 만들어 바꿔치기 하는것을 말한다. 

## Service(서비스)
* 어떤 연산이 1개의 객체에만 속하지 않는경우, 억지로 특정 객체에 넣는 것보다는 Service로 만드는 것이 좋다. 

억지로 특정 객체에 넣을경우 그 객체는 불필요한 의존성이 추가되어 개념적 명확성을 잃어버리고, 복잡해져서 리팩토링 하기 어려워진다. 

Service는 상태를 캡슣화하지 않고, 이를 호출하는 클라이언트에 제공하는 연산만 한다.

## Service와 격리된 도메인 계층
Service는 도메인 계층에만 허용 되는것은 아니다. 도메인, 응용서비스, 인프라스트럭쳐 모두 Service계층이 있다
> 문헌상에서 논의되는 Service는 대부분 순수하게 기술적이며 인프라스트럭쳐 계층에 속한다. 도메인 Service와 응용 Service는 인프라스트럭쳐 Service와 협업 하는데,

도메인서비스, 응용서비스는 계층은 Entity, VO로 만들어지기 때문에 구분하기가 어렵다.
> 예를 들어, 뱅킹 어플리케이션에서 거래를 분석할 수 있게 스프레드시트 파일로 거래내역을 변환해 내보내기 기능은 응용 Service에 해당한다. 은행 업무 도메인에서는 “파일 형식” 이라는 것은 아무런 의미가 없으며, 그것과 관련된 어떠한 업무 규칙도 없기 때문이다.

> 다른 계좌로 자금을 이체하는 기능은 도메인 Service 에 해당한다. 자금 이체 기능에는 중요한 업무규칙(가령, 출금 계좌에서 자금을 인출해서 입금하는) 이 포함돼 있고, “자금 이체” 는 중요한 은행업무 도메인의 용어이기 때문이다. 이 경우 Service 가 그 자체로는 많은 일을 하지 않으며, *두 Account(계좌)* 객체가 대부분의 일을 수행하도록 요청할 것이다. 그러나 이체 연산을 Account객체에 집어넣은 것은 다소 부자연스러울 수도 있는데, 왜냐하면 이체 연산은 두 계좌와 일부 전역적인 규칙을 수반하기 때문이다.

> 자금 이체와 관련된 규칙과 이력이 더해진 두 기입 내역을 나타내는 Funds Transfer(자금 이체) 객체를 생성하고 싶을지도 모른다. 하지만 그런 경우에도 은행간 네트워크에서 Service를 요청하지 않을수는 없다. 또한 대부분의 개발 시스템에서는 도메인 객체와 외부 자원간의 직접적인 인터페이스를 만든다는 것이 자연스러워보이진 않는다.

## 구성 단위
Service 계층을 설계하는것은 도메인 로직을 모아둔 것이기 때문에 Service계층의 인터페이스는 곧 도메인 계층의 인터페이스와 연결된다.

> 구성 단위가 중간크인(medium-granted) 무상태 Service는 대형 시스템에서 재사용하기거 더 쉬울수 있는데, 왜냐하면 그러한 서비스는 단순한 인터페이스 너머에 중요한 기능을 캡슐화하고 있기 때문이다. 아울러 구성 단위가 세밀한(fine-grained) 객체는 분산 시스템에서 비효율적인 메시지 전송을 초래할 수 있다.

구성단위가 세밀한 도메인객체는 다른 계층에서 도메인 로직을 처리할 수 있게된다.그러므로 도메인 Service의 인터페이스는 구성단위가 중간크기 여야 한다.

> 앞서 논의한 바와 같이 구성 단위가 세밀한 도메인 객체는 도메인에서 지식이 새어 나오게 해서 도메인 객체의 행위를 조정하는 응용 계층으로 흘러가게 할 수 있다. 그렇게 되면 고도로 세분화된 상호작용의 복잡성이 결국 응용 계층에서 처리되고, 도메인 계층에서 사라진 도메인 지식이 응용 계층의 코드나 사용자 인터페이스 계층의 코드로 스며든다. 도메인 서비스를 적절히 도입하면 계층 간의 경계를 선명하게 하는데 도움 될 수 있다.


# 모듈(Module)
모듈화를 하는 이유는 개발자의 인지적 과부화 때문이다.

코드에서 표현하는 개념의 갯수는 되도록 줄이고(결합도를 낮춘다), 단편적인 생각들의 일관성을유지하여(응집도는 높인다) 모듈을 구성해야 한다.
> Module 간에는 결합도가 낮아야 하고 Module의 내부는 응딥도가 높아야 한다는 것은 두말하면 잔소리다.

> Module 로 쪼개지는 것은 코드가 아닌 바로 개념이다. 어떤 사람이 한 번에 생각해낼 수 있는 양에는 한계가 있으며(따라서 결합도는 낮춰야 한다). 일관성이 없는 단편적인 생각은 획일적인 생각을 섞어놓은것처럼 이해하기 어렵다(따라서 응집도는 높여야 한다)

모듈은 연관관계와 상호작용을 기준으로 기계적으로 판단하여 만들지 못한다.

결합도가 높으면 하나의 같은 개념을 여러개의 모듈에 나눠서 설명하는 것이다. 그러면 이를 이해하는데 여러 모듈을 살펴봐야 하므로 이해하기 어렵게 된다.

> 두 모델 요소가 서로 다른 모듈로 분리될 때마다 각 요소는 이전에 비해 좀 더 간접적인 곽녜에 놓이는데, 이로써 설계에서 그와 같은 모델 요소의 위치를 파악하는 부담이 늘어난다.

## 인프라스트럭처 주도 패키지화의 함정
패키지 나누는 기준을 구현 기술중심으로 하게되면, 패키지로 나눠진 계층의 이해는 쉬울 수 있으나, 도메인 모델이 여러 계층으로 나눠지기 때문에, 도메인 모델의 파악이 어려워진다

> 한편으로 티어 아키텍처는 모델 객체에 대한 구현을 잘게 나눠서 서로 흩어지게 할 수도 있다. 어떤 프레임워크에서는 단일 도메인 객체의 책임을 여러 객체에 걸쳐 퍼트린다음 그러한 객체를 제각기 분리된 패키지에 두는 식으로 티어를 만들어 내기도 한다. 이를테면, J2EE의 일반 관행은 연관된 업무로직은 ‘세션 빈’ 에 두고 데이터와 데이터에 대한 접근은 ‘엔티티 빈’ 에 두는 것이다. 이렇게 되면 각 컴포넌트의 구현 복잡성이 증가하면서 그러한 분리로 객체 모델의 응집력을 이내 잃어버리게 된다. 객체의 가장 기본적인 개념중 하나는 데이터와 해당 데이터를 대상으로 연산을 수행하는 로직을 캡슐화 하는 것이다.
> 
> …
> 
> 다양한 객체를 바라보면서 머릿속으로 그것들을 역으로 하나의 개념적 ENTITY 로 끼워맞추려면 너무나도 많은 노력이 들 것이다.